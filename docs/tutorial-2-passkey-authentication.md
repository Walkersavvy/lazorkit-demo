# Tutorial 2 — Passkey Authentication

Covers WebAuthn on the web and native biometrics on mobile, registration, login, and session persistence.

Highlights:
- Web: use browser WebAuthn API; HTTPS required
- Mobile: use `@lazorkit/wallet-mobile-adapter` for native biometrics
- Shared hooks live in `packages/lazor-core`